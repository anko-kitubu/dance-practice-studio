# Dance練習ツール 仕様書（仮実装版）

## 1. 目的 / 概要
ダンス練習時に「YouTubeのお手本動画」と
「自分のカメラ映像」を同時に表示し、
比較しながら練習できる環境を提供する。

## 2. 対象ユーザー
- 自宅でダンス練習をする人
- 同じ動画を繰り返し確認したい人
- 鏡のように自分の動きを確認したい人

## 3. スコープ
### 3.1 対象（仮実装範囲）
- YouTube動画の読み込みと再生制御
- カメラ映像の表示とミラー切替
- レイアウト切替（Split / Reverse /
  Videoのみ / Cameraのみ）
- 最終状態の保存と復元（localStorage）

### 3.2 対象外
- 履歴 / マイリスト
- フローティング表示、リサイズ
- 録画 / リプレイ
- クラウド同期 / ログイン

## 4. 機能仕様
### 4.1 YouTube再生
- 入力：YouTube URL または 動画ID
- 入力値が不正な場合はエラーメッセージを表示
- 再生操作：
  - 再生 / 一時停止
  - シークバーでの時間移動
  - 倍速変更（YouTube APIで取得できる範囲）
- YouTubeの標準UIは非表示（自作UIのみ）
- 再生状態が変化したらボタン表示を更新

### 4.2 カメラ
- 端末のカメラ映像を表示
- 初期状態はミラー表示
- ミラー表示のON/OFF切替
- 起動できない場合はエラーメッセージを表示

### 4.3 レイアウト
以下の4種類を切り替え可能：
- Split（左：動画 / 右：カメラ）
- Reverse（左：カメラ / 右：動画）
- Videoのみ
- Cameraのみ

### 4.4 永続化
localStorageに以下を保存し、次回起動時に復元する：
- 動画ID
- 入力欄の値（URL or ID）
- 再生速度
- レイアウト
- ミラー設定

## 5. UI仕様
### 5.1 画面構成
- 上部：コントロールバー
- 下部：動画/カメラの表示エリア

### 5.2 コントロール
- YouTube URL入力欄 + Loadボタン
- 再生/停止ボタン
- シークバー + 時間表示
- 倍速セレクト
- レイアウト切替ボタン
- ミラー切替トグル

## 6. 非機能要件 / 制約
- 対応環境：PCの最新Chrome / Edge想定
- `file://` 直開きは不可（ES Modules /
  カメラ制約のため）
  - ローカルサーバー経由で実行すること
- YouTube埋め込み制限のある動画は再生不可
- 自動再生はブラウザ制限に依存

## 7. データ保存仕様
保存先：`localStorage`
キー：`dance.practice.state`

保存データ例：
```json
{
  "videoId": "FgGJ323GlUk",
  "lastInput": "https://youtu.be/FgGJ323GlUk",
  "playbackRate": 1,
  "layout": "split",
  "mirrorCamera": true
}
```

## 8. 受け入れ条件（MVP）
- URL/IDを入力してLoadすると動画が切り替わる
- Playで再生、再度押すと停止する
- シークバーで任意位置に移動できる
- 倍速を変更すると即座に反映される
- カメラ映像が表示され、ミラー切替が効く
- レイアウト切替が即座に反映される
- 再読み込み後も設定が復元される

## 9. 実装構成（移行しやすい分割）
- `Dance/index.html`：画面の骨組み
- `Dance/styles.css`：スタイル
- `Dance/app.js`：状態管理・イベント・全体制御
- `Dance/services/youtube.js`：YouTube IFrame APIラッパー
- `Dance/services/camera.js`：カメラ制御
- `Dance/services/storage.js`：永続化
