# ダンス練習Webアプリ：音声波形 + カメラ姿勢推定（上半身）連動エフェクト まとめ

## 目的
- 画面下部に「波形（waveform）」を表示する。
- カメラ映像からユーザの動き（主に上半身）を推定し、その“動き量”で波形や別オブジェクトを連動させる（例：振幅/太さ/揺れ/発光/パーティクル量）。

---

## 音声取得（「今流れている音」からリアルタイム波形）
### 現実的な取得方法
A. **画面共有経由で音声をキャプチャ**（推奨：ブラウザで鳴っている音を対象にする）
- `navigator.mediaDevices.getDisplayMedia({ video: true, audio: true })` で、ユーザーに「共有対象（タブ/画面）」を選ばせる。取得結果は `MediaStream`。  
  - `getDisplayMedia()` 自体はユーザー許可が必須。HTTPS等の安全なコンテキストが必要。  
  - 参照: MDN getDisplayMedia :contentReference[oaicite:0]{index=0}
- OS/ブラウザで挙動が違う：
  - Windows/ChromeOS：全画面共有時に「システム音声」まで取れるケースがある
  - Linux/macOS：基本「タブ音声」しか取れない（全システム音は不可になりがち）
  - 参照: caniuse（getDisplayMedia audio capture support） :contentReference[oaicite:1]{index=1}

B. **ユーザーが音声/動画ファイルをアップロード**して波形生成（安定・確実）
- `<input type="file">` → `AudioContext.decodeAudioData()` → peaks計算 → Canvas描画
- YouTube等の音声“抽出”は避け、必要なら「YouTube再生（別） + 波形（手元音源）」で同期だけ取る。

---

## 波形の生成/描画（リアルタイム）
- Web Audio API で `AnalyserNode` を使い、波形データを取得して Canvas に描く。
  - `analyser.getByteTimeDomainData(Uint8Array)` を `requestAnimationFrame` でループ。
  - 参照: MDN（Visualizations / getByteTimeDomainData） :contentReference[oaicite:2]{index=2}
- UI案：
  - 画面下部固定の Canvas（例：高さ 120〜200px）
  - 波形は中央線から上下に描画
  - 「現在位置カーソル」（縦線）を重ねられる設計にする（後述同期と相性良）

---

## YouTube同期（任意：動画再生位置と波形カーソルを合わせたい場合）
- YouTubeは IFrame Player API の `player.getCurrentTime()` で再生秒を取れる。  
  - 参照: YouTube IFrame Player API :contentReference[oaicite:3]{index=3}
- 波形Canvas上のカーソル位置 = (currentTime / duration) * canvasWidth
- ※音声データ自体をYouTubeから取り出すのではなく、「位置同期」に限定する。

---

## カメラから動きを捉える（姿勢推定）
### 推奨：MediaPipe Pose Landmarker（Web）
- `@mediapipe/tasks-vision` の Pose Landmarker を利用。
- `runningMode: "VIDEO"` + `detectForVideo(videoEl, timestamp)` で毎フレーム推論。  
  - 参照: 公式ガイド（Web/JS） :contentReference[oaicite:4]{index=4}
- 出力ランドマークは全身だが、**上半身だけ使用**すればOK（肩〜肘〜手首、必要なら胴体基準点）。
- 重要：上半身だけ使っても推論コスト自体は大きくは減らない。軽量化は入力解像度/推論頻度で調整。

### 上半身だけの運用方針
- 利用ランドマーク例：
  - 肩(11,12) / 肘(13,14) / 手首(15,16) / 指先(17〜22 任意)
  - 体幹安定用に腰/股関節(23,24)を補助的に使うのもあり
- フレームアウト/遮蔽/暗所で不安定になるため、可視性(visibility)が低い点は除外する。

---

## “動き量”の定義（Pose → 数値）
- 基本：前フレームとの差分（速度）を合算して「モーションエナジー」を作る。
- 手首/肘/肩の速度の総和（あるいは二乗和）を採用。
- ガタつき対策：
  - EMA（指数移動平均）で平滑化
  - 推論は 15〜30fps に間引く（描画は60fpsでも良い）

疑似：
- energy = Σ hypot(dx, dy) over selected joints
- energySmoothed = ema(prev, energy, alpha)

---

## 波形/オブジェクトへのマッピング（面白さの中核）
- 波形（画面下部）のパラメータに motionEnergy を掛ける：
  - 波形の振幅スケール（動くほど大きい）
  - 線幅（動くほど太い）
  - 追加の“うねり”成分（sinの位相進行を動きで加速）
  - 発光/粒子/残像（動くほど強い）
- 併用も可：
  - 音声波形（ベース） + 動き量（エフェクト）で見た目を変える

---

## Vue + TS 実装スケルトン（推奨構成）
### コンポーネント
- `WaveformCanvas.vue`
  - props: `audioLevel`, `motionEnergy`, `currentTime?`, `duration?`
  - Canvas描画（下部固定）
- `CameraPose.vue`
  - `<video>`（カメラ） + デバッグ用にランドマーク描画（任意）
- `Controls.vue`
  - Start/Stop、キャプチャ種別選択（画面共有/ファイル/なし）

### Composable
- `useDisplayAudioCapture()`
  - `start()` → getDisplayMedia、音声track検出、AudioContext/Analyser生成
  - `getWaveformFrame()` / `getRMS()` など
- `usePoseLandmarker()`
  - `init(modelPath)` / `start(videoEl)` / `stop()`
  - `getLandmarks()`、`getMotionEnergy()`

---

## ループ設計（負荷を制御）
- 描画ループ（60fps）：Canvas更新（波形/エフェクト）
- Pose推論ループ（15〜30fps）：`detectForVideo` → motionEnergy更新
- Audio解析（60fps or 30fps）：`getByteTimeDomainData` 取得

---

## 制約/注意点
- `getDisplayMedia` の音声取得は環境依存（特に macOS/Linux のシステム音は基本不可）。 :contentReference[oaicite:5]{index=5}
- すべて権限要求が必要（ユーザー操作起点で開始）。
- 安定性は「画角（上半身が常に入る）」「照明」で大きく変わる。
